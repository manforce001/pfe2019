/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AjouteRucheService } from './ajouteRuche.service';

describe('Service: AjouteRuche', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AjouteRucheService]
    });
  });

  it('should ...', inject([AjouteRucheService], (service: AjouteRucheService) => {
    expect(service).toBeTruthy();
  }));
});
