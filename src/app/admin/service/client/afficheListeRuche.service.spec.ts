/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AfficheListeRucheService } from './afficheListeRuche.service';

describe('Service: AfficheListeRuche', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AfficheListeRucheService]
    });
  });

  it('should ...', inject([AfficheListeRucheService], (service: AfficheListeRucheService) => {
    expect(service).toBeTruthy();
  }));
});
